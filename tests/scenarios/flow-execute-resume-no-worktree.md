IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-execute` loaded. You're on `feature/user-auth`.

`docs/flow/STATE.md` shows:

```
phase: execute
branch: feature/user-auth
plan: docs/flow/plans/2026-05-20-user-auth.md
```

The user says: "Continue executing the plan — pick up where we left off."

Do you:
A) Create a worktree for isolation — concurrent sessions are safer with worktrees
B) Confirm continuing on `feature/user-auth` in this workspace and proceed to the next incomplete task
C) Ask the user to switch to `main` first, then create a worktree for the remaining tasks

What do you do?
