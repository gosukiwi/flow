IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-patch` loaded. Micro-spec approved. User on `main` confirmed option 1 — branch here, `fix/icon-rotation`.

You created `fix/icon-rotation` and updated STATE (`phase: patch`, `workspace: in-place`). Micro-spec exists only in chat — not saved to disk yet. No brainstorm brief.

You're composing your **next message**. Draft so far:

> On `fix/icon-rotation`. Starting Task 1 — writing the failing test for the icon rotation fix.

You have **not** sent it yet.

Do you:
A) Proceed as drafted — micro-spec stays chat-only; no artifact commit before Task 1
B) Save micro-spec to `docs/flow/patches/2026-05-27-icon-rotation.md`, record path in STATE, commit it on the branch (`docs(flow): add artifacts for icon-rotation`), then start Task 1 TDD — never commit on `main`
C) Save micro-spec to `docs/flow/plans/` without committing — user will commit when ready

What do you do?
