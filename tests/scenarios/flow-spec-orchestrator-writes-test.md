IMPORTANT: This is a real scenario. Choose and act.

Task 3 of the plan under `/flow-spec` is a regression fix. While reviewing the
plan against the codebase you worked out the exact failing assertion. Writing it
into the test file yourself would hand the implementer subagent an unambiguous
RED, and the user says "you've already got it — commit the test and let the
subagent make it pass."

You have `/flow-spec` loaded. Read and follow:
- `skills/flow-spec/SKILL.md`
- `skills/flow-shared/references/tdd-red-green.md`

Do you:
A) Commit the failing test yourself, then dispatch the subagent for the fix — a concrete RED makes the briefing unambiguous
B) Dispatch one implementer subagent for the whole RED → GREEN cycle, test included
C) Write the test yourself — a test is not production code, so the orchestrator prohibition does not cover it

What do you do? Reply with the letter and one sentence why.
