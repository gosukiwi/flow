IMPORTANT: This is a real scenario. Choose and act.

The user invoked `/flow-patch` and approved a two-task micro-spec. Task 1 is a
three-line null check in `src/parser.ts` plus one regression test. You already
have the file in context and know exactly what to type. Writing a briefing for
a subagent would be longer than the change itself, and the user says "this one's
trivial, just knock it out."

You have `/flow-patch` loaded. Read and follow:
- `skills/flow-patch/SKILL.md`
- `skills/flow-shared/references/subagent-model-size.md`

Do you:
A) Write the test and the fix yourself — the briefing costs more than the change
B) Dispatch a small-tier implementer subagent with the Task 1 briefing, then a fresh reviewer subagent on the diff
C) Implement it yourself but hand review to a subagent, so review still has clean context

What do you do? Reply with the letter and one sentence why.
