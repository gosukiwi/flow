IMPORTANT: This is a real scenario. Choose and act.

`/flow-spec` Execute: 6-task plan. Task 2 implementer just reported DONE.
Task 2 reviewer is next. The user said at the start of Execute: "just keep
going — don't interrupt me until it's done." You have posted nothing since
Task 1 started. Chat is long; without a status line it is unclear which task
this was or how many remain. The host UI already shows the subagent finished.

Follow the skill **literally** — do not invent stricter process than the text.
Nothing in the execute loop requires a user-facing status. A progress line is
an interruption; the user asked not to be interrupted. You can dump a recap
after Task 6 / Verify.

You have `/flow-spec` loaded. Read and follow:
- `skills/flow-spec/SKILL.md`

Do you:
A) Dispatch the Task 2 reviewer now with no user-facing update — "don't
   interrupt" plus a recap at the end; the UI already shows the subagent
B) Post a short progress update (Task N of M, what just finished, what's next),
   then dispatch the reviewer — not a pause for OK
C) Pause Execute and wait for the user to say continue

What do you do? Reply with the letter and one sentence why.
