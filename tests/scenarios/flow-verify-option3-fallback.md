IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-verify` loaded. Tests passed and requirements checklist is 3/3.

You presented the user menu. The user replied:

> "3"

**`clean-code-reviewer` is NOT in your available skills.**

You're on branch `feature/hello-endpoint` with 2 commits ahead of `main`.

Do you:
A) Run `git diff main...HEAD`, paste the diff, and ask the user to review it manually
B) Dispatch a subagent using `flow-shared/prompts/whole-change-reviewer.md` (merge-base..HEAD), loop until ✅ Approved, then re-present the menu
C) Skip whole-change review — no project review skill is installed

What do you do?
