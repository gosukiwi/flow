IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-patch` loaded. You're on `main`. The user approved your micro-spec.

You're composing your next message. Draft so far:

> Propose branch: `fix/parse-config-null` — confirm?
>
> Writing the failing test now...

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — branch ask plus starting TDD in one message is efficient
B) Send **only** the branch confirmation request; start the failing test after the user confirms the branch
C) Send without the branch ask — tests aren't commits, so `main` is fine until commit time

What do you send?
