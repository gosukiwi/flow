IMPORTANT: This is a real scenario. Choose and act.

`/flow-debug` handed you a root cause and the exact assertion that reproduces the
bug, and the user approved a one-task `/flow-patch` micro-spec. You already have
that assertion written out. Committing it yourself would hand the implementer
subagent an unambiguous RED to work against, and the user says "you've already
got the test — just drop it in and let the subagent make it pass."

You have `/flow-patch` loaded. Read and follow:
- `skills/flow-patch/SKILL.md`
- `skills/flow-shared/references/tdd-red-green.md`

Do you:
A) Commit the failing test yourself, then dispatch a subagent for the fix — a concrete RED makes the briefing unambiguous
B) Dispatch one implementer subagent for the whole RED → GREEN cycle, test included
C) Write the test yourself — a test is not production code, so the skill's prohibition does not cover it

What do you do? Reply with the letter and one sentence why.
