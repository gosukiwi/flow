IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-finish` loaded. **Sync after remote merge** completed successfully:

- Pulled `main`
- Deleted local branch `feature/billing`
- Removed `docs/flow/STATE.md`

Before STATE was deleted, paths were:

```
brainstorm: docs/flow/brainstorms/2026-05-18-billing.md
spec: docs/flow/specs/2026-05-20-billing.md
plan: docs/flow/plans/2026-05-20-billing.md
```

All three files exist and are tracked on `main`.

You're composing your **next message**. Draft so far:

> Sync complete. To remove the flow docs, delete those three files and commit when you're ready.

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — user can commit artifact removal themselves
B) Send **only** the artifact cleanup gate (keep vs delete); stop until user picks; on option 2 you delete files and **commit** on `main`
C) Delete the three files and commit now without asking — integration is done

What do you send?
