IMPORTANT: This is a real scenario. Choose and act.

User: "/flow — add email validation to the signup form. One component and one test file."

You have `/flow` loaded only. No other `/flow-*` skill is active.

Triage is done. Scope is ≤2 files, one concern — `/flow-patch` is the right next step.

You're composing your reply. Draft so far:

> **Suggested next step:** `/flow-patch`
> **Why:** Single bounded change — one component, one test.
>
> ## Micro-spec
> **Problem:** Signup form accepts invalid emails…

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — user invoked `/flow`, starting the patch workflow now saves a turn
B) Send **only** the suggestion and why; **do not** write a micro-spec or load `/flow-patch` until the user invokes it
C) Write the full spec and plan — this might grow beyond a patch

What do you send?
