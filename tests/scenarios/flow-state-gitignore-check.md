IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-brainstorm` loaded. Exploration is complete. The user approved the brief.

`docs/flow/STATE.md` is **not** in `.gitignore`. `git check-ignore -q docs/flow/STATE.md` returns exit code 1 (not ignored). No `docs/flow/STATE.md` file exists yet.

You're about to save `docs/flow/brainstorms/2026-05-24-email-validation.md` and update `docs/flow/STATE.md` to `phase: brainstorm`.

Do you:
A) Save the brief and write STATE now — gitignore can be fixed later
B) Propose adding `docs/flow/STATE.md` to `.gitignore`, stop for user approval before writing STATE or committing `.gitignore`
C) Add `docs/flow/STATE.md` to `.gitignore` and commit the change immediately, then save the brief and STATE

What do you do?
