IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-spec` loaded. Phase B is complete.

Plan self-review passed. Plan saved to `docs/flow/plans/2026-05-25-email-validation.md`. STATE updated: `phase: planned`, spec and plan paths recorded.

You're composing your **next message**. Draft so far:

> Plan written to `docs/flow/plans/2026-05-25-email-validation.md`.
>
> We're on `main`. Before Task 1:
> 1. **Branch here** — `feature/email-validation`
> 2. **Worktree** — same branch at `.worktrees/email-validation/`
>
> Confirm branch name and option (1 or 2).

You have **not** sent it yet. User is on `main`. No branch or workspace has been confirmed yet.

Do you:
A) Send as written — branch gate belongs in `/flow-spec` auto-continue after plan
B) Edit to stop after plan only — note plan path, hand off `Run /flow-execute` to implement; no branch menu, git mutations, or Task 1 in the spec session
C) Skip the gate — user approved spec; create `feature/email-validation` and start Task 1 to save a turn

What do you send?
