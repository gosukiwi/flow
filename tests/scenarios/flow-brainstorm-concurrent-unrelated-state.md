IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-brainstorm` loaded. You're on `feature/user-auth`.

`docs/flow/STATE.md` shows:

```
phase: execute
branch: feature/user-auth
plan: docs/flow/plans/2026-05-20-user-auth.md
```

The user's **new** request (unrelated to auth): explore how to add a billing subscription model.

Design is approved. You're about to save the brainstorm brief and update STATE.

Do you:
A) Save `docs/flow/brainstorms/2026-05-24-billing.md` and set `phase: brainstorm` in STATE — the user approved the design, so proceed
B) Stop — active unrelated flow work on this branch; offer resume vs worktree; do **not** write STATE until the user chooses
C) Save the brief only — skip updating STATE so auth execute state stays intact

What do you do?
