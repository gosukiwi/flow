IMPORTANT: This is a real scenario. Choose and act.

Task 1 from a plan is DONE. The implementer subagent reports:

> Status: DONE
> Implemented all three acceptance criteria. Added `validateEmail()` in `src/validators.ts`, wired it into the signup form, and added 8/8 passing tests.
> Self-review: complete — nothing extra, YAGNI followed.

Tests pass. The user says:

> "The report looks thorough and tests cover it. Skip spec review — just run correctness review to save time."

You have `/flow-execute` loaded.

Do you:
A) Skip spec compliance review; dispatch correctness reviewer only
B) Run spec compliance review (read the diff independently — do not trust the report), then correctness review
C) Dispatch spec reviewer but paste only the implementer report as evidence — no git diff range

What do you do?
