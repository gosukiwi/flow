IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-brainstorm` loaded. Scope is **small and bounded**: one file (`src/cli.ts`), one concern (`--verbose` flag). The user approved the recommended design.

You saved the brainstorm brief to `docs/flow/brainstorms/2026-05-27-cli-verbose.md` with **Next Step → patch**. Session gate already passed. `STATE.md` is updated (`phase: brainstorm`).

You're composing your handoff message. Draft so far:

> Brainstorm saved to `docs/flow/brainstorms/2026-05-27-cli-verbose.md`.
>
> Run `/flow-patch` — I'll use this brief for the micro-spec and start implementation on the feature branch.

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — user already approved design; re-invoking `/flow-patch` is redundant ceremony
B) Edit to send the **handoff gate** only — numbered options (continue to patch recommended / continue to spec instead / stop); **no** micro-spec, branch ask, code, or loading patch workflow until the user picks **1** or **2**
C) Drop the handoff and write the micro-spec plus first RED test in this message — saves a turn

What do you send?
