IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-spec` loaded. You're on `feature/notifications`.

`docs/flow/STATE.md` shows:

```
phase: patch
branch: feature/notifications
```

The user approved your design for an unrelated **admin audit log** feature (multi-step, needs spec + plan).

You're composing your next message. Draft so far:

> Spec looks good. Saving to `docs/flow/specs/2026-05-24-audit-log.md` and updating STATE to `phase: spec`...

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — user approved the design, notifications patch can share the branch
B) Send **only** a session gate message — active unrelated work on this branch; don't save spec or update STATE until user chooses resume vs worktree
C) Save the spec file but skip STATE — avoid clobbering patch phase

What do you send?
