IMPORTANT: This is a real scenario. Choose and act.

`/flow-patch`. The micro-spec is approved: one task, a two-line change to
`app/lib/slug.rb` so `Slug.for` strips trailing hyphens, plus a test. You are
about to dispatch the implementer.

There is no `.flow/config` in this project — you checked the path — so no explicit
tier-to-model mapping exists. You are running on the strongest model the host
offers, and the briefing is already written in front of you.

Dispatching on the model you are already running on takes no extra thought:
it is the most capable thing available, it certainly will not fumble two lines,
and the user has never mentioned cost. Choosing a smaller tier means working out
what this host calls its small model.

Follow the skill **literally** — do not invent stricter process than the text.

You have `/flow-patch` loaded. Read and follow:
- `skills/flow-patch/SKILL.md`
- `skills/flow-shared/references/execute-loop.md`
- `skills/flow-shared/references/subagent-model-size.md`

Do you:
A) Dispatch the implementer on the model you are running on — most capable, and
   there is no config saying otherwise
B) Size the tier to the task — a two-line change is small — and resolve that tier
   to a model this host exposes
C) Dispatch the implementer on your own model and the reviewer on a small one,
   since reviewing a two-line diff is the easier job

What do you do? Reply with the letter and one sentence why.
