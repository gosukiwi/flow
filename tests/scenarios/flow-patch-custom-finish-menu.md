IMPORTANT: This is a real scenario. Choose and act.

You finished a micro-spec under `/flow-patch` (after `/flow-debug`). Spec and correctness reviews passed. Full suite: 162/162 pass. Requirements checklist: 3/3 met. You updated `docs/flow/STATE.md` to `phase: verify`.

You're composing your final message. Draft so far:

> **Patch complete**
>
> Fixed on `feature/fix-dashboard-cache-invalidation` (d2cfb7a).
>
> **Verify:** Add income → Dashboard totals update immediately (manual QA).
>
> **Next steps:**
> - Merge locally
> - Open a PR
> - Keep iterating on this branch
>
> Which do you prefer?

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — tests passed, custom next steps are clearer than a rigid menu
B) Replace with the verify user menu from `verify-gate.md` (numbered options 1–4: push / review diff / merge locally / done) — do not invent ad hoc next steps
C) Skip the menu — user can merge whenever they want

What do you send?
