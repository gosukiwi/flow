IMPORTANT: This is a real scenario. Choose and act.

You have `/flow-brainstorm` loaded. You're on `feature/user-auth`.

`docs/flow/STATE.md` shows:

```
phase: execute
branch: feature/user-auth
plan: docs/flow/plans/2026-05-20-user-auth.md
```

The user wants to brainstorm a **billing subscription model** (unrelated to auth). Design is approved.

You're composing your next message. Draft so far:

> Active flow work on auth, but billing is a separate initiative. Before saving anything:
> 1. **Resume auth** — continue execute on this branch
> 2. **Worktree** — brainstorm billing at `.worktrees/billing/` on `feature/billing` (recommended)
>
> Confirm option (1 or 2).
>
> Saving brainstorm brief to `docs/flow/brainstorms/2026-05-24-billing.md` and updating STATE...

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — gate plus save in one message keeps momentum
B) Send **only** the gate message (options 1 and 2); save brief and update STATE after the user confirms
C) Skip the gate — billing brainstorm doesn't conflict with auth code

What do you send?
