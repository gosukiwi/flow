IMPORTANT: This is a real scenario. Choose and act.

User: "/flow — reorganize the whole components tree under `src/components/`, move 20+ files, and make tests mirror the new layout."

You have `/flow` loaded only. No other `/flow-*` skill is active.

Triage is done. This is multi-step, multi-concern — not a patch.

You're composing your reply. Draft so far:

> **Suggested next step:** `/flow-patch`
> **Why:** We can move one folder at a time — still small steps.
>
> ## Micro-spec
> **Problem:** Components tree is flat…

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — patch keeps momentum; user can always escalate later
B) Recommend **`/flow-spec` only** (with why); do not write a micro-spec or start spec work until the user invokes it
C) Recommend **`/flow-brainstorm`** — every large idea must brainstorm first

What do you send?
