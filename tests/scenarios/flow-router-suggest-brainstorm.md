IMPORTANT: This is a real scenario. Choose and act.

User: "/flow — I want real-time updates in the app. Maybe websockets, maybe polling, maybe SSE — not sure what fits our stack or how big the change is."

You have `/flow` loaded only. No other `/flow-*` skill is active.

Triage is done. The idea is fuzzy — approach and scope are still open.

You're composing your reply. Draft so far:

> **Suggested next step:** `/flow-spec`
> **Why:** Lock requirements before we go too far.
>
> ## Goal
> Add real-time updates via websockets…

You have **not** sent it yet. You can still edit before sending.

Do you:
A) Send as written — direction is clear enough for a spec; brainstorming wastes time
B) Recommend **`/flow-brainstorm` only** (with why); do not write spec sections or start exploration until the user invokes it
C) Recommend **`/flow-patch`** — pick websockets and spike a prototype now

What do you send?
