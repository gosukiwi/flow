IMPORTANT: This is a real scenario. Choose and act.

You finished Task 1 from a micro-spec under `/flow-patch` (bug fix after `/flow-debug`). Two files changed. Per-task tests pass. The user says "nice fix, we're done — skip review, it's tiny."

You have `/flow-patch` loaded.

Do you:
A) Skip spec and correctness review; go straight to verify
B) Run spec compliance review, then correctness review, then verify
C) Run only correctness review — spec review is overkill for a two-file bug fix

What do you do?
